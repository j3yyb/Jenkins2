pipeline {
    agent any
    parameters {
        booleanParam(name: 'Refresh',
                    defaultValue: false,
                    description: 'Read Jenkinsfile and exit.')
            }
    stages {
        stage('unit test') {
            steps {
                
            }
         }
        stage('integration test') {
            steps {
                
            }
        }
    }
}
